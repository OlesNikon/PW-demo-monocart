# Test info

- Name: Login form tests >> Verify that user can login with valid credentials
- Location: /home/runner/work/PW-demo-monocart/PW-demo-monocart/tests/login-form.spec.ts:5:7

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).toBeVisible()

Locator: locator('[data-test="title"]').getByText('Products')
Expected: visible
Received: <element(s) not found>
Call log:
  - expect.toBeVisible with timeout 5000ms
  - waiting for locator('[data-test="title"]').getByText('Products')

    at /home/runner/work/PW-demo-monocart/PW-demo-monocart/tests/login-form.spec.ts:9:46
```

# Page snapshot

```yaml
- text: Swag Labs
- textbox "Username": undefined
- textbox "Password": undefined
- 'heading "Epic sadface: Username and password do not match any user in this service" [level=3]':
  - button
  - text: "Epic sadface: Username and password do not match any user in this service"
- button "Login"
- heading "Accepted usernames are:" [level=4]
- text: standard_user locked_out_user problem_user performance_glitch_user error_user visual_user
- heading "Password for all users:" [level=4]
- text: secret_sauce
```

# Test source

```ts
   1 | import { test, expect } from '../fixtures/baseFixture';
   2 | import { STANDARD_USER, INVALID_USER } from '../data/credentials';
   3 |
   4 | test.describe('Login form tests', { tag: ['@smoke', '@login'] }, () => {
   5 |   test('Verify that user can login with valid credentials', { tag: ['@1-1-1'] }, async ({ app }) => {
   6 |     await app.login.open();
   7 |     await app.login.login(STANDARD_USER);
   8 |
>  9 |     await expect(app.products.productsTitle).toBeVisible();
     |                                              ^ Error: Timed out 5000ms waiting for expect(locator).toBeVisible()
  10 |   });
  11 |
  12 |   test('Verify that user can not login with invalid credentials', { tag: ['@1-1-2'] }, async ({ app }) => {
  13 |     await app.login.open();
  14 |     await app.login.login(INVALID_USER);
  15 |
  16 |     await expect(app.login.errorAlert).toHaveText(
  17 |       'Epic sadface: Username and password do not match any user in this service'
  18 |     );
  19 |   });
  20 |
  21 |   test('Verify that user can not login without credentials', { tag: ['@1-1-3'] }, async ({ app }) => {
  22 |     await app.login.open();
  23 |     await app.login.submitLogin();
  24 |
  25 |     await expect(app.login.userNameInputField).not.toHaveClass(/error/);
  26 |     await expect(app.login.passwordInputField).toHaveClass(/error/);
  27 |   });
  28 | });
  29 |
```