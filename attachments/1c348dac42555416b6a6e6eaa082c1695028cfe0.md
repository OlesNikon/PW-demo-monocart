# Test info

- Name: Login form tests >> Verify that user can not login without credentials
- Location: /home/runner/work/PW-demo-monocart/PW-demo-monocart/tests/login-form.spec.ts:30:7

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).not.toHaveClass(expected)

Locator: locator('[data-test="username"]')
Expected pattern: not /error/
Received string: "input_error form_input error"
Call log:
  - expect.not.toHaveClass with timeout 5000ms
  - waiting for locator('[data-test="username"]')
    9 × locator resolved to <input value="" type="text" id="user-name" name="user-name" autocorrect="off" data-test="username" autocapitalize="none" placeholder="Username" class="input_error form_input error"/>
      - unexpected value "input_error form_input error"

    at /home/runner/work/PW-demo-monocart/PW-demo-monocart/tests/login-form.spec.ts:34:52
```

# Page snapshot

```yaml
- text: Swag Labs
- textbox "Username"
- textbox "Password"
- 'heading "Epic sadface: Username is required" [level=3]':
  - button
  - text: "Epic sadface: Username is required"
- button "Login"
- heading "Accepted usernames are:" [level=4]
- text: standard_user locked_out_user problem_user performance_glitch_user error_user visual_user
- heading "Password for all users:" [level=4]
- text: secret_sauce
```

# Test source

```ts
   1 | import { test, expect } from '../fixtures/baseFixture';
   2 | import { STANDARD_USER, INVALID_USER } from '../data/credentials';
   3 |
   4 | test.describe('Login form tests', { tag: ['@smoke', '@login'] }, () => {
   5 |   /**
   6 |   * @testrail 1
   7 |   */
   8 |   test('Verify that user can login with valid credentials', async ({ app }) => {
   9 |     await app.login.open();
  10 |     await app.login.login(STANDARD_USER);
  11 |
  12 |     await expect(app.products.productsTitle).toBeVisible();
  13 |   });
  14 |
  15 |   /**
  16 |   * @testrail 2
  17 |   */
  18 |   test('Verify that user can not login with invalid credentials', async ({ app }) => {
  19 |     await app.login.open();
  20 |     await app.login.login(INVALID_USER);
  21 |
  22 |     await expect(app.login.errorAlert).toHaveText(
  23 |       'Epic sadface: Username and password do not match any user in this service'
  24 |     );
  25 |   });
  26 |
  27 |   /**
  28 |   * @testrail 3
  29 |   */
  30 |   test('Verify that user can not login without credentials', async ({ app }) => {
  31 |     await app.login.open();
  32 |     await app.login.submitLogin();
  33 |
> 34 |     await expect(app.login.userNameInputField).not.toHaveClass(/error/);
     |                                                    ^ Error: Timed out 5000ms waiting for expect(locator).not.toHaveClass(expected)
  35 |     await expect(app.login.passwordInputField).toHaveClass(/error/);
  36 |   });
  37 | });
  38 |
```